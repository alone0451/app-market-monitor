# 更新记录

## v1.1.1

- 安装包获取能力扩展：无网页直链的渠道，在手机已连接且已安装目标应用时，支持**从手机提取已安装 APK** 并自动校验包名、版本、SHA-256 和签名。
- 优化安装包获取提示：明确标注网页直链下载支持范围（应用宝/百度/360）；未连接实体手机、目标应用未安装等场景给出准确原因，不再误报“未安装市场客户端”。
- 修复网页直链下载失效问题：下载任务实时采集结果此前未真正返回，导致应用宝/百度/360 的官方直链一直没被使用；现已修复并验证可正常从官方网页下载。
- 百度手机助手：兼容 detailBusiness* 版式的详情卡片（此前只认 detailNature*），并增加 appDoc 主体兜底；修复部分应用被误判为未收录的问题。
- 荣耀：部分应用的最近更新日期位于介绍展开后的下方，采集时增加向下滚动查找，避免漏采发布日期。
- 360 手机助手：搜索接口不含时间字段，新增从移动端详情页（m.app.so.com）读取“更新时间”并解析发布日期。

## v1.1.0

- 补齐 OPPO、vivo、荣耀的版本发布日期：
  - 荣耀：详情页自动展开“更多”读取最近更新日期；
  - vivo：直接读取更新时间（如 2026-07-13）；
  - OPPO：读取相对时间（如“1 个月前”）并换算为近似日期。
- 监测市场配置区新增“本次全选 / 本次清空”按钮，与上方监测清单的操作保持一致。
- 优化监测配置页：打开时不再自动发起候选搜索，仅恢复上次输入，由用户确认后搜索。
- 应用宝：未收录的包名返回 HTTP 404 时按“未上架”处理，不再误报为渠道查询失败。
- OPPO：详情页读到版本后补读一屏，抓取开发者公司主体（如示例金融信息服务有限公司）。
- 风险判定优化：非授权渠道只有在“检测到该应用版本”时才标高风险；未发现该应用则不构成风险（同名错包仍按严重风险处理）。
- 三星：接口返回错误码/空包名时按“未收录或已下架”处理，不再误判为同名错包严重风险。
- 修复 OPPO、vivo、荣耀应用市场手机端采集不到版本号的问题：
  - 修正荣耀深链协议（`honormarket://app_details`），包名直达详情页不再回落首页；
  - 增加页面加载等待与重试，解决市场客户端冷启动慢、UI 抓取过早导致空结果的问题；
  - UI 抓取失败时自动重试并清理残留 dump，避免把旧页面误当当前页面。

## v1.0.0

- 支持 Android 与 iOS 应用市场版本巡检、历史记录和风险汇总。
- 支持按 App 名称或公司名称发现候选，并按平台分别建立监测对象。
- 统一展示版本、发布时间、监测时间、开发者和运营主体。
- 支持版本不一致、非授权渠道、同名错包和疑似仿冒风险提示。
- 提供 macOS 一键部署：自动创建虚拟环境、安装 Python 依赖并下载项目独立的 Android Platform-Tools。
- 提供换机兼容性诊断：识别实体手机、系统版本和已安装市场客户端。
- USB 手机仅用于渠道详情复核和截图，不自动下载或安装目标 App。
- 默认排除本地配置、数据库、APK、截图、日志、构建密钥和测试结果。
